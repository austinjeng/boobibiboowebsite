# Explore Bingo Map（快樂探索 Bingo × 情境避雷地圖）需求文件 PRD

> 版本：v0.1（MVP 定義）  
> 文件目的：整理產品目標、核心玩法、功能需求、資料模型與開發里程碑，供兩人協作開發使用。

---

## 1. 產品概述（Overview）

### 1.1 一句話定位
**用 Bingo 任務引導城市探索，並用「快樂標籤」記錄回憶；「注意事項」作為可選備註，形成情境化的探索地圖。**

### 1.2 核心理念
- **Bingo 任務永遠是「快樂/中性探索」**，不鼓勵為了完成任務而追求不愉快體驗。
- 地圖的主角是 **快樂體驗**；避雷資訊只作為 **溫柔提醒（選填）**，不做負面導向的「踩雷收集」。

---

## 2. 目標與非目標

### 2.1 產品目標（Goals）
1. 讓使用者在城市探索中獲得快樂與成就感（Bingo 完成感）。
2. 讓使用者能留下「回憶式打卡」，形成專屬的探索地圖（Memory Map）。
3. 提供可篩選的情境建議（約會/安靜聊天/拍照/放空），降低踩雷機率但不破壞愉悅氛圍。
4. 支援輕量好友互動（房間比拼進度），提升可玩性。

### 2.2 非目標（Non-goals）
- 不做「公開評分/一星負評」導向的評論站。
- 不追求完整社交平台（追蹤、動態牆、留言戰場）於 MVP 階段。
- MVP 不強制串接第三方地點資料 API（可先用手動建點/輸入）。

---

## 3. 使用者與情境

### 3.1 目標使用者（Personas）
- **雙人探索者**：情侶/朋友一起出門，希望有任務感、也想留下回憶。
- **想避免踩雷的人**：希望用「注意事項」作為輕量提醒，提升行程成功率。
- **偏收藏派**：喜歡把去過的地方整理成可回顧的地圖/相簿。

### 3.2 主要使用情境
- 週末想出門但不知道去哪 → 打開 Bingo 任務卡挑一格
- 打卡一個快樂瞬間 → 留下照片/一句話 + 快樂標籤
- 下次想約會/找安靜地方 → 用情境篩選地圖
- 與好友（或彼此）比拼 → 看排行榜與完成線數

---

## 4. 核心玩法與主循環（Core Loop）

1. 取得一張 Bingo 任務卡（例：5×5）
2. 選一格任務去完成（探索型/快樂感受型/收集型/互動型）
3. 在地圖或搜尋選地點 → 打卡
4. 打卡必填「快樂標籤」並可選填「注意事項」
5. 完成任務格子 + 地圖新增回憶點位
6. 累積完成線 → 解鎖成就/展示排行（房間制）

---

## 5. 功能需求（Functional Requirements）

> 本節以 MVP 為主，並列出後續可擴充項目（V1/V2）。

### 5.1 Bingo 任務卡（Explore Bingo）

#### 5.1.1 MVP
- 顯示 5×5 任務卡（25 格）
- 每格顯示：任務標題/簡述、完成狀態
- 點格子可進入「完成任務」流程（導向打卡頁）
- 任務完成後：
  - 格子顯示完成（勾勾/填色）
  - 記錄完成時間與對應 Check-in

#### 5.1.2 V1/V2 延伸
- 每週主題任務卡（例如：約會週、散步週、拍照週）
- 任務卡切換：本週/歷史
- 任務推薦：系統挑「下一個最適合完成」的任務

---

### 5.2 打卡（Check-in）

#### 5.2.1 MVP（最小可用）
**必填**
- 地點（名稱 + 座標，至少一種方式取得）
- 快樂標籤：至少選 1 個
- 照片或一句話（二選一）

**選填**
- 注意事項（偏吵、排隊、低消、桌距近…）
- 情境標籤（約會/親子/安靜工作/朋友/放空）

**完成後**
- 新增一筆 Check-in 記錄
- 完成對應 Bingo 格子（若從任務格進入）

#### 5.2.2 V1/V2 延伸
- 多張照片
- 自動抓時間與天氣（可選）
- 允許「回填」：先打卡、後綁定任務格

---

### 5.3 地圖（Memory Map + 情境提醒）

#### 5.3.1 MVP
- 地圖呈現所有打卡點位
- 點位卡片（列表或地圖彈窗）顯示：
  - 地點名稱
  - 快樂標籤（主顯示）
  - 一句話心得（或照片縮圖）
- 篩選與搜尋：
  - 依快樂標籤篩選（例：安靜、氣氛好、拍照好看）
  - 依情境標籤篩選（例：約會、朋友聚會）
  - 地點搜尋（名稱）
- 注意事項呈現方式：
  - **預設收合**（不把雷點放前面）
  - 點開詳情才顯示注意事項

#### 5.3.2 V1/V2 延伸
- 同地點聚合統計（多人/多次打卡）
- 地圖熱點視覺（依標籤密度）
- 分享某點位的回憶卡片連結

---

### 5.4 好友比拼（Rooms）

#### 5.4.1 MVP（最小可行）
- 房間機制（Room code）
- 加入房間（輸入 code）
- 房間排行：
  - 完成格子數
  - 完成線數（Bingo line）
- 顯示同房間成員進度摘要

#### 5.4.2 V1/V2 延伸
- 房間內共享地圖（只看同房間點位）
- 私密「黑名單共享」（可選、且注意語氣）

---

### 5.5 成就系統（Achievements）

#### 5.5.1 MVP
- 完成一條線 → 解鎖成就卡（純文字/徽章）
- 個人頁可看到已解鎖成就列表

#### 5.5.2 V1/V2 延伸
- 成就分享圖（自動產生分享卡）
- 稀有成就（例如：連續三週完成一條線）

---

## 6. 標籤字典（Taxonomy）

### 6.1 快樂標籤（必填，主角）
建議 MVP 先做 10 個以內：
- 氣氛很讚
- 甜點驚喜
- 店員親切
- 很適合聊天
- 很安靜
- 拍照好看
- 風景很好
- CP 值高
- 很療癒
- 想回訪

> UI 建議：快樂標籤用「明亮、正向」視覺，優先顯示。

### 6.2 注意事項（選填，配角）
建議 MVP 先做 6–8 個：
- 可能偏吵
- 可能排隊久
- 低消/規則多
- 桌距近
- 上菜慢
- 交通不便
- 座位少
- 價格偏高

> UI 建議：注意事項預設收合，避免破壞整體氛圍。

### 6.3 情境標籤（選填）
- 約會
- 親子
- 安靜工作
- 一個人放空
- 朋友聚會

---

## 7. 任務卡內容（範例 5×5，快樂導向）

> MVP 可直接使用此套任務卡（可寫死），後續再做自動生成與每週主題。

1. 去一個沒去過的街區散步 20 分鐘  
2. 找到一個光線很好的拍照點  
3. 吃到一樣你們都覺得滿分的東西  
4. 找到一個能坐超過 1 小時不趕人的地方  
5. 收集一張你們最喜歡的街景照  

6. 找到一間氣氛很舒服的店  
7. 發現一個你們想回訪的地點  
8. 嘗試一個你們平常不會點的品項  
9. 找到一個適合聊天的座位環境  
10. 今天的店用 3 個詞形容它  

11. 收集一個「意外很喜歡」的小驚喜  
12. 用 100 字寫下今天最快樂的瞬間  
13. 找到一個你們會想帶朋友去的地方  
14. 做一次角色互換：你選地點/我選餐點  
15. 找到一個讓你們覺得「很療癒」的角落  

16. 收集一個「安靜」標籤地點  
17. 收集一個「拍照好看」標籤地點  
18. 收集一個「甜點值得」標籤地點  
19. 收集一個「散步順路」標籤地點  
20. 今日 MVP 地點投票（並留下原因）  

21. 找到一個「CP 值很好」的選擇  
22. 找到一個「你們想二刷」的店/景點  
23. 兩人各拍一張今天最滿意的照片  
24. 打卡一個「讓人心情變好」的地方  
25. 完成一條 Bingo 線（任意五格）

---

## 8. 資訊架構（IA）與頁面

### 8.1 頁面列表（MVP）
1. **首頁（Home）**
   - 本週任務卡摘要（完成格數、完成線數）
   - 快速入口：開始打卡 / 看地圖 / 看任務卡
2. **任務卡頁（Bingo Board）**
   - 5×5 格子
   - 點格子 → 進入打卡流程（帶任務資訊）
3. **打卡頁（Check-in）**
   - 地點選擇（搜尋/地圖點選/手動輸入）
   - 快樂標籤（必填）
   - 照片或一句話（二選一）
   - 注意事項（選填）
4. **地圖頁（Map）**
   - 點位顯示
   - 快樂標籤篩選 / 情境篩選 / 搜尋
5. **房間/排行榜（Room & Leaderboard）**
   - 加入房間（輸入 code）
   - 排行榜與進度比較
6. **個人頁（Profile）**
   - 我的打卡列表
   - 成就徽章（可簡化）

---

## 9. 資料模型（Data Model）

> 建議使用 PostgreSQL / MySQL；或 NoSQL 亦可。以下以關聯式描述。

### 9.1 User
- id (PK)
- name
- avatarUrl (nullable)
- createdAt

### 9.2 Place
- id (PK)
- name
- lat
- lng
- externalPlaceId (nullable) 例如 Google Place ID
- createdAt

### 9.3 CheckIn
- id (PK)
- userId (FK)
- placeId (FK)
- note (text, nullable)
- photoUrl (nullable)
- happyTags (string[]) 例如 ["安靜","氣氛很讚"]
- cautions (string[]) 例如 ["可能排隊久","低消"]
- contexts (string[]) 例如 ["約會","安靜聊天"]
- createdAt

### 9.4 BingoCard
- id (PK)
- theme (string) 例如 "快樂探索"
- weekStartDate (date, nullable for MVP)
- createdAt

### 9.5 BingoTask
- id (PK)
- bingoCardId (FK)
- index (0~24)
- title
- rule (text)
- createdAt

### 9.6 BingoCompletion
- id (PK)
- userId (FK)
- bingoTaskId (FK)
- checkInId (FK)
- completedAt

### 9.7 Room
- id (PK)
- code (unique)
- bingoCardId (FK)
- createdAt

### 9.8 RoomMember
- id (PK)
- roomId (FK)
- userId (FK)
- joinedAt

---

## 10. API 需求（MVP 級別）

> 依技術栈調整（REST/GraphQL 皆可）。

### 10.1 任務卡
- `GET /bingo/current`：取得本週任務卡 + 使用者完成狀態
- `POST /bingo/complete`：以 check-in 完成指定 task

### 10.2 地點與打卡
- `POST /places`：新增地點（手動輸入）
- `GET /places/search?q=...`：搜尋地點
- `POST /checkins`：新增打卡
- `GET /checkins?filters=...`：查詢打卡（支援標籤/情境篩選）

### 10.3 地圖
- `GET /map/points?filters=...`：取得地圖點位資料（可用 checkins 聚合）

### 10.4 房間
- `POST /rooms`：建立房間（回傳 code）
- `POST /rooms/join`：加入房間
- `GET /rooms/{code}/leaderboard`：房間排行榜

---

## 11. 驗收標準（Acceptance Criteria）

### 11.1 Bingo 任務卡
- 使用者可看到 25 格任務與完成狀態
- 完成打卡後對應任務格會顯示完成

### 11.2 打卡
- 打卡必須至少選 1 個快樂標籤
- 打卡成功後：
  - 地圖出現新點位
  - 任務完成狀態更新（若綁定任務）

### 11.3 地圖
- 可瀏覽所有點位
- 可依快樂標籤篩選
- 注意事項預設不主動顯示（需點開詳情）

### 11.4 房間排行
- 可建立房間並取得 code
- 其他使用者可加入
- 排行榜能顯示完成格數或完成線數

---

## 12. 風險與解法（Risks & Mitigations）

1. **任務主觀難判定**  
   - 解法：MVP 先以可判定任務為主（例如「打卡一個你願意回訪的地點」仍可用自我判定，但不要太抽象）
2. **地點資料來源不穩**  
   - 解法：MVP 允許手動建立 Place；後續再串 Google Places
3. **避雷資訊導致負面氛圍**  
   - 解法：注意事項「選填 + 收合 + 語氣中性」

---

## 13. 兩人分工（建議）

### 前端（A）
- Bingo 任務卡 UI（格子、完成狀態）
- 打卡頁（表單、標籤選擇、圖片上傳 UI）
- 地圖頁（點位顯示、篩選器、點位詳情）
- 房間排行頁（列表與進度顯示）

### 後端（B）
- Auth（MVP 可簡化：email magic link/Google OAuth）
- Places/CheckIn CRUD
- BingoCompletion 邏輯
- Room & Leaderboard 聚合
- 檔案存儲（照片上傳）

---

## 14. 開發里程碑（1～2 週 Demo 建議）

### Milestone 1：可展示 MVP（第 1 週）
- 任務卡（固定一張）
- 打卡（文字 + 快樂標籤）
- 地圖顯示點位
- 任務格完成狀態更新

### Milestone 2：作品感加強（第 2 週）
- 篩選（快樂標籤 + 情境）
- 房間排行（code 加入）
- 成就卡（完成線解鎖）

### Milestone 3：產品化（後續）
- 每週主題任務卡自動切換
- 分享連結（點位/任務卡）
- 同地點多次打卡聚合統計

---

## 15. UI/UX 原則（建議）

- **快樂標籤永遠在第一視覺層**（主角）
- 注意事項預設收合，不做負面打擊式呈現
- 打卡流程應做到 **30 秒內完成**（降低摩擦）
- 任務卡要有明確的「進度感」與「完成回饋」（動畫/顏色/勾勾）

---

## 16. 附錄：標籤常數（建議代碼 Key）

### happyTags keys（示例）
- vibe_good
- dessert_surprise
- friendly_service
- good_for_chat
- quiet
- photogenic
- great_view
- good_value
- healing
- want_revisit

### cautions keys（示例）
- noisy
- long_queue
- min_charge
- crowded
- slow_service
- hard_to_reach
- few_seats
- pricey

### contexts keys（示例）
- date
- family
- work
- solo
- friends

---
